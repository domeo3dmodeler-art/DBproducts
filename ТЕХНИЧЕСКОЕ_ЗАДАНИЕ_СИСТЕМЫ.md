# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Система управления данными о товарах от поставщиков (PIM System)

**Версия:** 2.0  
**Дата:** 2024  
**Статус:** Production Ready

---

## 1. EXECUTIVE SUMMARY

### 1.1. Назначение системы

Система представляет собой **Product Information Management (PIM) платформу** для централизованного управления информацией о товарах, поступающей от множества поставщиков. Система обеспечивает:

- **Стандартизацию** данных о товарах от различных источников
- **Автоматическую верификацию** полноты, качества и медиа-контента
- **Управление жизненным циклом** товаров от импорта до экспорта
- **Версионирование** и отслеживание истории изменений
- **Гибкую систему атрибутов** для различных типов товаров

### 1.2. Бизнес-ценность

1. **Сокращение времени обработки** данных о товарах на 70-80%
2. **Повышение качества** данных за счет автоматической валидации
3. **Стандартизация** формата данных от всех поставщиков
4. **Прозрачность** процесса обработки через workflow и версионирование
5. **Масштабируемость** для работы с сотнями поставщиков и тысячами товаров

### 1.3. Целевая аудитория

| Роль | Задачи | Доступ |
|------|--------|--------|
| **Менеджер по закупкам** | Управление поставщиками, категориями, подкатегориями | Полный доступ к справочникам |
| **Специалист по данным** | Импорт данных, настройка маппинга, массовые операции | Импорт, товары, верификация |
| **Менеджер по качеству** | Проверка и утверждение товаров, управление workflow | Товары, верификация, workflow |
| **Администратор** | Настройка системы, управление атрибутами, пользователями | Полный доступ |
| **Поставщик** (будущее) | Заполнение шаблонов, загрузка данных | Ограниченный доступ к своим товарам |

---

## 2. АРХИТЕКТУРА СИСТЕМЫ

### 2.1. Концептуальная модель данных

```
┌─────────────────────────────────────────────────────────────┐
│                    КАТАЛОГ ТОВАРОВ                          │
│                  (первичная структура)                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ├─── Категория (ProductCategory)
                            │    ├── Код: "01"
                            │    ├── Название: "Сантехника"
                            │    └── Описание
                            │
                            └─── Подкатегория (Subcategory)
                                 ├── Код: "01_1"
                                 ├── Название: "Смесители"
                                 ├── Категория → ProductCategory
                                 └── Эталонные атрибуты
                                     ├── Атрибут 1 (обязательный)
                                     ├── Атрибут 2 (опциональный)
                                     └── ...

┌─────────────────────────────────────────────────────────────┐
│                    ПОСТАВЩИКИ                               │
│              (привязываются к категориям)                   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ├─── Поставщик (Supplier)
                            │    ├── Код: "SUP001"
                            │    ├── Название: "ООО Акваме"
                            │    ├── Контакты
                            │    └── Связи:
                            │        ├── Категории (many-to-many)
                            │        └── Подкатегории (many-to-many)
                            │
                            └─── Товар (Product)
                                 ├── SKU (уникальный)
                                 ├── Название
                                 ├── Подкатегория → Subcategory
                                 ├── Статус (workflow)
                                 └── Значения атрибутов
                                     ├── Атрибут 1: "Значение 1"
                                     ├── Атрибут 2: "Значение 2"
                                     └── ...
```

### 2.2. Ключевые принципы архитектуры

#### 2.2.1. Каталог первичен
- **Категории и подкатегории** создаются независимо от поставщиков
- Каталог определяет **структуру данных** (эталонные атрибуты)
- Поставщики **привязываются** к существующим категориям/подкатегориям

#### 2.2.2. Эталонные свойства
- Каждая подкатегория имеет **строго определенный набор атрибутов**
- Атрибуты могут быть **обязательными** или **опциональными**
- При импорте проверяются **только эталонные свойства**
- Лишние данные из файла поставщика **игнорируются**

#### 2.2.3. Гибкая система атрибутов
- **Глобальный справочник атрибутов** с уникальными названиями
- Атрибуты имеют **типы** (текст, число, дата, булево, URL, изображение, выбор, 3D модель)
- Атрибуты могут иметь **единицы измерения**
- Атрибуты могут быть **уникальными** (не допускают дублирования значений)
- Поддержка **вариантов выбора** для типа SELECT

#### 2.2.4. Автоматическая нумерация
- **Категории**: автоматическая генерация кода (01, 02, 03...)
- **Подкатегории**: код включает код категории (01_1, 01_2, 02_1...)
- **Поставщики**: автоматическая генерация кода (SUP001, SUP002...)

---

## 3. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 3.1. Управление каталогом товаров

#### 3.1.1. Категории товаров

**Функционал:**
- ✅ Создание, редактирование, удаление категорий
- ✅ Автоматическая генерация кода
- ✅ Управление статусом активности
- ✅ Поиск и фильтрация

**Поля:**
- `code` (String, уникальный) - код категории (01, 02...)
- `name` (String, обязательное) - название
- `description` (Text, опциональное) - описание
- `is_active` (Boolean) - статус активности

**Бизнес-правила:**
- Код должен быть уникальным глобально
- Нельзя удалить категорию, если у неё есть подкатегории или поставщики (блокировка удаления)
- При деактивации категории (`is_active = False`):
  - Деактивируются все подкатегории категории
  - Деактивируются все товары в подкатегориях категории
  - Новые подкатегории в эту категорию создать нельзя
  - Можно реактивировать категорию, тогда подкатегории и товары также активируются
- При удалении категории (если нет подкатегорий и поставщиков):
  - Удаляются все связи с поставщиками
  - Удаляются все запросы данных (`DataRequest`) по этой категории (только со статусом `new` или `cancelled`)

#### 3.1.2. Подкатегории товаров

**Функционал:**
- ✅ Создание, редактирование, удаление подкатегорий
- ✅ Автоматическая генерация кода (с учетом категории)
- ✅ Привязка к категории
- ✅ Управление эталонными атрибутами
- ✅ Поиск и фильтрация

**Поля:**
- `code` (String, уникальный в рамках категории) - код подкатегории (01_1, 01_2...)
- `name` (String, обязательное) - название
- `description` (Text, опциональное) - описание
- `category_id` (Integer, ForeignKey) - категория
- `is_active` (Boolean) - статус активности

**Бизнес-правила:**
- Код должен быть уникальным в рамках категории (не может быть двух подкатегорий с одинаковым кодом в одной категории)
- Нельзя удалить подкатегорию, если у неё есть товары (блокировка удаления)
- При деактивации подкатегории (`is_active = False`):
  - Все товары подкатегории деактивируются (не удаляются)
  - Новые товары в эту подкатегорию создать нельзя
  - Можно реактивировать подкатегорию, тогда товары также активируются
- При удалении подкатегории (если нет товаров):
  - Удаляются все связи с поставщиками
  - Удаляются все связи с атрибутами (`SubcategoryAttribute`)
  - Удаляются все запросы данных (`DataRequest`), включающие эту подкатегорию (только со статусом `new` или `cancelled`)

#### 3.1.3. Эталонные атрибуты подкатегории

**Функционал:**
- ✅ Добавление атрибутов из глобального справочника
- ✅ Настройка обязательности
- ✅ Настройка порядка сортировки
- ✅ Удаление атрибутов из подкатегории

**Поля связи (SubcategoryAttribute):**
- `subcategory_id` (Integer, ForeignKey)
- `attribute_id` (Integer, ForeignKey)
- `is_required` (Boolean) - обязательность
- `sort_order` (Integer) - порядок сортировки

**Бизнес-правила:**
- Атрибут может быть добавлен в подкатегорию только один раз
- Нельзя удалить обязательный атрибут, если у подкатегории есть товары с заполненными значениями

### 3.2. Управление поставщиками

#### 3.2.1. Поставщики

**Функционал:**
- ✅ Создание, редактирование, удаление поставщиков
- ✅ Автоматическая генерация кода
- ✅ Привязка к категориям (many-to-many)
- ✅ Привязка к подкатегориям (many-to-many)
- ✅ Поиск и фильтрация

**Поля:**
- `code` (String, уникальный) - код поставщика (SUP001, SUP002...)
- `name` (String, обязательное) - название
- `contact_person` (String, опциональное) - контактное лицо
- `email` (String, опциональное) - email
- `phone` (String, опциональное) - телефон
- `address` (Text, опциональное) - адрес
- `is_active` (Boolean) - статус активности

**Бизнес-правила:**
- Код должен быть уникальным глобально
- Поставщик должен быть привязан хотя бы к одной категории (при создании)
- Нельзя удалить поставщика, если у него есть товары (блокировка удаления)
- При деактивации поставщика (`is_active = False`):
  - Все товары поставщика деактивируются (не удаляются)
  - Новые запросы данных для этого поставщика создать нельзя
  - Существующие запросы остаются активными
  - Можно реактивировать поставщика, тогда товары также активируются
- При удалении поставщика (если нет товаров):
  - Удаляются все связи с категориями и подкатегориями
  - Удаляются все запросы данных (`DataRequest`) со статусом `new` или `cancelled`
  - Запросы со статусом `request_sent`, `data_received` или `no_response` не удаляются (для истории)

#### 3.2.2. Генерация шаблонов для поставщиков

**Функционал:**
- ✅ Генерация Excel шаблона для категории и поставщика
- ✅ Отдельный лист для каждой подкатегории
- ✅ Предзаполненные заголовки с атрибутами
- ✅ Инструкции по заполнению
- ✅ Валидация данных (выпадающие списки)
- ✅ Примеры заполнения

**Структура шаблона:**
1. **Лист "📋 ИНСТРУКЦИЯ"**:
   - Общая информация о поставщике
   - Подробные инструкции
   - Список подкатегорий

2. **Листы подкатегорий**:
   - Заголовки: базовые поля (SKU, название, описание) + атрибуты
   - Обязательные поля помечены красным фоном
   - Необязательные поля помечены зеленым фоном
   - Выпадающие списки для SELECT атрибутов
   - Строка с примером (нужно удалить перед заполнением)

**Бизнес-правила:**
- Шаблон содержит только подкатегории, привязанные к выбранному поставщику
- Если указана категория, шаблон содержит только подкатегории этой категории
- Атрибуты отображаются с единицами измерения (если есть)

### 3.3. Управление атрибутами

#### 3.3.1. Глобальный справочник атрибутов

**Функционал:**
- ✅ Создание, редактирование, удаление атрибутов
- ✅ Управление типами атрибутов
- ✅ Настройка единиц измерения
- ✅ Настройка правил валидации
- ✅ Управление вариантами выбора (для SELECT)
- ✅ Умный импорт атрибутов из файлов
- ✅ Импорт атрибутов из буфера обмена
- ✅ Поиск и фильтрация

**Поля:**
- `code` (String, уникальный) - код атрибута (генерируется автоматически из названия через транслитерацию)
- `name` (String, уникальный, обязательное) - название (русское)
- `type` (Enum) - тип атрибута
- `description` (Text, опциональное) - описание
- `unit` (String, опциональное) - единица измерения
- `is_unique` (Boolean) - уникальность значений (глобально для всех товаров)
- `validation_rules` (JSON, опциональное) - правила валидации

**Бизнес-правила:**
- Название атрибута должно быть уникальным глобально (нельзя создать два атрибута с одинаковым названием)
- Код атрибута генерируется автоматически из названия (транслитерация русского в английский)
- Нельзя удалить атрибут, если он используется в подкатегориях (есть записи в `SubcategoryAttribute`)
- Нельзя изменить тип атрибута, если он используется в товарах (есть записи в `ProductAttributeValue`)
- При изменении типа атрибута требуется переверификация всех товаров, использующих этот атрибут

**Типы атрибутов:**
- `TEXT` - текстовое значение
- `NUMBER` - числовое значение
- `DATE` - дата
- `BOOLEAN` - булево значение (Да/Нет)
- `URL` - ссылка
- `IMAGE` - изображение (URL)
- `SELECT` - выбор из списка
- `3D_MODEL` - 3D модель (URL)

**Бизнес-правила:**
- Название атрибута должно быть уникальным
- Код атрибута генерируется автоматически из названия (транслитерация)
- Нельзя удалить атрибут, если он используется в подкатегориях
- Нельзя изменить тип атрибута, если он используется в товарах

#### 3.3.2. Умный импорт атрибутов

**Функционал:**
- ✅ Загрузка атрибутов из Excel/CSV файлов
- ✅ Вставка атрибутов из буфера обмена
- ✅ Предпросмотр данных перед импортом
- ✅ Автоматический маппинг с существующими атрибутами
- ✅ Предложение новых атрибутов
- ✅ Настройка единиц измерения
- ✅ Валидация перед импортом

**Процесс импорта:**
1. Загрузка файла или вставка из буфера
2. Предпросмотр данных (заголовки, примеры строк)
3. Маппинг колонок:
   - Автоматическое сопоставление с существующими атрибутами
   - Предложение новых атрибутов
   - Настройка единиц измерения
   - Редактирование названий колонок
4. Валидация маппинга
5. Импорт атрибутов

**Бизнес-правила:**
- При совпадении названия создается связь с существующим атрибутом
- При несовпадении создается новый атрибут
- Единицы измерения должны быть совместимы (если атрибут уже существует)

### 3.4. Импорт товаров

#### 3.4.1. Поддерживаемые форматы

- ✅ Excel (.xlsx, .xls) - многостраничные файлы
- ✅ CSV
- ✅ JSON

#### 3.4.2. Процесс импорта

**Шаги:**
1. Выбор поставщика и подкатегории
2. Загрузка файла
3. Автоматическое определение листа (по названию подкатегории)
4. Автоматический маппинг полей
5. Предпросмотр данных (опционально)
6. Импорт товаров
7. Автоматическая верификация
8. Отображение результатов

**Обработка данных:**
- Товары создаются со статусом "Черновик" (draft)
- Сразу после успешного создания товар автоматически переходит в статус "В работе" (in_progress)
- Автоматическая валидация обязательных полей
- Проверка типов данных
- Сохранение только эталонных свойств
- Пропуск строки с примером (если есть)
- Создание первой версии товара
- Связывание товара с файлом импорта через `import_history_id`

**Результаты импорта:**
- Количество импортированных товаров
- Список ошибок (с указанием строки)
- Список предупреждений
- Ссылки на созданные товары

#### 3.4.3. Автоматический маппинг полей

**Алгоритм:**
1. Точное совпадение названий колонок с кодами атрибутов
2. Частичное совпадение (нормализация: нижний регистр, убрать пробелы)
3. Маппинг стандартных полей (SKU → sku, Название → name, Описание → description)
4. Несопоставленные поля игнорируются

### 3.5. Верификация товаров

#### 3.5.1. Автоматическая верификация

**Проверка полноты данных (40% веса):**
- Наличие всех обязательных атрибутов
- Заполненность значений (не пустые)
- Расчет оценки: `(заполненных / обязательных) * 100%`

**Проверка качества данных (40% веса):**
- Соответствие типов данных
- Валидация по правилам атрибутов
- Проверка уникальности (если атрибут помечен как уникальный)
- Проверка дублирования по SKU
- Расчет оценки: `100% - (ошибок / всего атрибутов) * 100%`

**Проверка медиа-контента (20% веса):**
- Проверка доступности URL фотографий (HTTP запрос с таймаутом 10 секунд)
- Проверка разрешения фотографий (минимум 800x600, загрузка и проверка через Pillow)
- Проверка формата и размера файлов (поддерживаемые форматы: JPG, PNG, WEBP; максимум 10MB)
- Проверка количества фотографий (минимум 1 для товара)
- Проверка 3D моделей (если применимо):
  - Проверка доступности URL
  - Проверка формата (GLB, GLTF)
  - Проверка размера (максимум 50MB)
- **Обработка ошибок:**
  - При недоступном URL: создается `VerificationIssue` с типом `MEDIA_ERROR`, товар не блокируется
  - При таймауте: создается `VerificationIssue`, товар не блокируется
  - При неверном формате: создается `VerificationIssue`, товар не блокируется
  - При превышении размера: создается `VerificationIssue`, товар не блокируется
  - Импорт продолжается даже при ошибках медиа (товар создается с предупреждениями)
- Расчет оценки: `(валидных медиа / всего медиа) * 100%`

**Общая оценка:**
```
overall_score = completeness_score * 0.4 + quality_score * 0.4 + media_score * 0.2
```

#### 3.5.2. Результаты верификации

**Сохранение:**
- Объект `ProductVerification` с оценками
- Список проблем (`VerificationIssue`)
- Типы проблем:
  - `MISSING_REQUIRED` - отсутствует обязательный атрибут
  - `INVALID_TYPE` - неверный тип данных
  - `VALIDATION_ERROR` - ошибка валидации
  - `UNIQUE_VIOLATION` - нарушение уникальности
  - `DUPLICATE_SKU` - дублирование SKU
  - `MEDIA_ERROR` - ошибка медиа-контента

**Отображение:**
- Общая оценка товара
- Детализация по категориям (полнота, качество, медиа)
- Список проблем с указанием атрибутов
- Рекомендации по исправлению

### 3.6. Workflow файлов от поставщиков

#### 3.6.1. Назначение

Система управляет **полным жизненным циклом файлов от поставщиков** от момента получения до экспорта данных в основную БД. Это обеспечивает прозрачность процесса обработки и контроль на каждом этапе.

#### 3.6.2. Статусы файлов

| Статус | Код | Описание |
|--------|-----|----------|
| В обработке | `processing` | Файлы, которые импортируются и готовятся (создаются автоматически при получении файла от поставщика) |
| В каталоге | `in_catalog` | Данные загружены в каталог, товары созданы в системе |
| Экспортировано | `exported` | Данные выгружены в основную БД |
| Ошибка | `failed` | Ошибка при обработке файла |

**Примечание:** Статус `pending` удален из workflow, так как в новой концепции "Сбор данных" файлы сразу создаются со статусом `processing` при получении от поставщика через запрос данных (`DataRequest`).

#### 3.6.3. Workflow переходов

**Автоматические переходы:**
- Файл создается со статусом `processing` при получении от поставщика (через "Отметить получено")
- `processing → in_catalog`: при успешном завершении импорта (хотя бы один товар создан)
- `processing → failed`: при критической ошибке импорта (не удалось создать ни одного товара)
- `in_catalog → exported`: при экспорте всех товаров в основную БД (только товары со статусом `approved`)

**Ручные переходы:**
- `failed → processing`: "Повторить импорт"
- `in_catalog → exported`: "Экспортировать в БД" (только если все товары в статусе `approved`)
- `exported → in_catalog`: "Откатить экспорт" (опционально, только для администраторов)

**Правила переходов:**
- Файл не может быть удален, если у него есть товары
- Файл не может перейти в `exported`, если не все товары в статусе `approved`
- При откате экспорта товары возвращаются в статус `approved`

#### 3.6.4. Связь с товарами

**Логика связи:**
- Файл импорта связан с товарами через `import_history_id` в модели `Product`
- Файл переходит в `in_catalog`, когда хотя бы один товар успешно создан (независимо от статуса товара)
- Файл переходит в `exported`, когда все товары экспортированы в основную БД
- Товары могут иметь разные статусы в рамках одного файла (draft, in_progress, to_review, approved, rejected)

**Двойной workflow:**
- **Workflow файла**: управляет процессом обработки файла
  - `processing` → `in_catalog` → `exported`
- **Workflow товара**: управляет жизненным циклом отдельного товара
  - `draft` → `in_progress` → `to_review` / `approved` / `rejected` → `exported`

**Правила экспорта:**
- Файл может быть экспортирован только если все товары в статусе `approved`
- При экспорте файла все товары переходят в статус `exported`
- Товары со статусом `rejected` не могут быть экспортированы
- Товары со статусом `draft` или `in_progress` не могут быть экспортированы

#### 3.6.5. Вкладки на главной странице

Главная страница содержит **4 вкладки** для управления полным циклом данных от поставщиков:

**1. Сбор данных:**
- **Назначение:** Управление процессом сбора данных от поставщиков от регистрации до получения файлов
- **Функционал:**
  - Создание и управление поставщиками
  - Привязка поставщиков к категориям (можно к нескольким)
  - Отправка запросов данных поставщикам
  - Отслеживание получения информации
- **Разделы:**
  - **Поставщики:** таблица/карточки с информацией о поставщиках, их категориях, статусах запросов
  - **Запросы данных:** таблица запросов с информацией о поставщике, категориях/подкатегориях, статусе, сроках
- **Действия:**
  - "Создать поставщика"
  - "Отправить запрос" (выбор поставщика, категории, подкатегорий, срока)
  - "Скачать шаблон" (для отправки поставщику)
  - "Отметить получено" (при получении файла от поставщика)
- **Статусы запросов:**
  - `new` - Новый поставщик, запрос еще не отправлен
  - `request_sent` - Запрос отправлен поставщику
  - `data_received` - Данные получены от поставщика
  - `no_response` - Нет ответа от поставщика
  - `cancelled` - Запрос отменен
- **Фильтры:** по поставщику, категории, статусу запроса, сроку

**2. В обработке (`processing`):**
- Файлы, которые импортируются и готовятся
- Информация: название, поставщик, запрос данных (ссылка), прогресс импорта, статус
- Индикаторы: прогресс-бар, количество обработанных строк
- Действия: "Отменить", "Просмотреть логи", "Повторить"
- Фильтры: по поставщику, запросу данных, статусу

**3. В каталоге (`in_catalog`):**
- Импорты, данные которых загружены в каталог
- Информация: файл, поставщик, запрос данных (ссылка), количество товаров, дата импорта, оценка верификации
- Действия: "Просмотреть товары", "Экспортировать в БД", "Повторная верификация"
- Фильтры: по поставщику, запросу данных, оценке верификации, статусу товаров

**4. Экспортировано (`exported`):**
- Экспорты данных в основную БД
- Информация: файл, поставщик, запрос данных (ссылка), количество товаров, дата экспорта, статус
- Действия: "Просмотреть детали", "Повторить экспорт", "Откатить"
- Фильтры: по поставщику, запросу данных, дате экспорта, статусу

#### 3.6.6. Модель данных

**Новая модель DataRequest (Запрос данных):**

Модель для отслеживания запросов данных от поставщиков по категориям и подкатегориям.

```python
class DataRequestStatus(enum.Enum):
    """Статусы запросов данных"""
    NEW = 'new'                    # Новый поставщик, запрос еще не отправлен
    REQUEST_SENT = 'request_sent'   # Запрос отправлен поставщику
    DATA_RECEIVED = 'data_received' # Данные получены от поставщика
    NO_RESPONSE = 'no_response'     # Нет ответа от поставщика
    CANCELLED = 'cancelled'         # Запрос отменен

class DataRequest(db.Model):
    """Запрос данных от поставщика"""
    __tablename__ = 'data_requests'
    
    id = db.Column(db.Integer, primary_key=True)
    supplier_id = db.Column(db.Integer, db.ForeignKey('suppliers.id'), nullable=False)
    category_id = db.Column(db.Integer, db.ForeignKey('product_categories.id'), nullable=False)
    subcategory_ids = db.Column(db.JSON)  # Список ID подкатегорий
    
    status = db.Column(db.Enum(DataRequestStatus), default=DataRequestStatus.NEW)
    request_sent_at = db.Column(db.DateTime)  # Когда отправлен запрос
    data_received_at = db.Column(db.DateTime)  # Когда получены данные
    deadline = db.Column(db.DateTime)  # Срок получения данных
    
    requested_by_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)
    request_message = db.Column(db.Text)  # Текст запроса (опционально)
    response_message = db.Column(db.Text)  # Ответ поставщика (опционально)
    
    import_history_id = db.Column(db.Integer, db.ForeignKey('import_history.id'))
    
    created_at = db.Column(db.DateTime, default=datetime.utcnow, nullable=False)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # Связи
    supplier = db.relationship('Supplier', backref='data_requests')
    category = db.relationship('ProductCategory', backref='data_requests')
    requested_by = db.relationship('User', foreign_keys=[requested_by_id])
    import_file = db.relationship('ImportHistory', foreign_keys=[import_history_id])
```

**Бизнес-правила:**
- Один поставщик может иметь несколько запросов по разным категориям
- Один запрос может включать несколько подкатегорий одной категории
- **Валидация при создании запроса:**
  - Все выбранные подкатегории должны принадлежать выбранной категории
  - Все выбранные подкатегории должны быть привязаны к выбранному поставщику
  - Если подкатегория не привязана к поставщику, она не может быть включена в запрос
- **Связь с файлами:**
  - Один запрос → один файл (один к одному)
  - Если поставщик прислал файл, который покрывает несколько запросов, создается отдельный файл для каждого запроса
  - Если один запрос требует несколько файлов (например, для разных подкатегорий), создается несколько запросов
- **Статусы запроса:**
  - `new` → `request_sent`: при отправке запроса поставщику
  - `request_sent` → `data_received`: при получении файла от поставщика
  - `request_sent` → `no_response`: при просрочке срока (deadline)
  - `request_sent` → `cancelled`: при отмене запроса вручную
  - `no_response` → `request_sent`: при повторной отправке запроса
- **Частичное получение данных:**
  - Если запрос включает несколько подкатегорий, но получены данные только для части:
    - Запрос переходит в статус `data_received` (данные получены)
    - Создается отдельный запрос для непокрытых подкатегорий со статусом `request_sent`
    - Или создается новый запрос со статусом `new` для недостающих данных

**Расширение ImportHistory:**
```python
class ImportFileStatus(enum.Enum):
    PROCESSING = 'processing'  # Файл импортируется
    IN_CATALOG = 'in_catalog'  # Данные в каталоге (товары созданы)
    EXPORTED = 'exported'  # Экспортировано в основную БД
    FAILED = 'failed'  # Ошибка обработки

class ImportHistory(db.Model):
    # ... существующие поля ...
    file_status = db.Column(db.Enum(ImportFileStatus), default=ImportFileStatus.PROCESSING)
    data_request_id = db.Column(db.Integer, db.ForeignKey('data_requests.id'), nullable=True)  # Связь с запросом (может быть NULL для файлов без запроса)
    exported_at = db.Column(db.DateTime)
    exported_by_id = db.Column(db.Integer, db.ForeignKey('users.id'))
    
    # Связь с запросом данных (один к одному)
    data_request = db.relationship('DataRequest', foreign_keys=[data_request_id], backref='import_file')
    
    # Бизнес-правила:
    # - Файл создается со статусом PROCESSING при получении от поставщика
    # - Файл может быть создан без запроса (прямая загрузка), тогда data_request_id = NULL
    # - Если файл создан через запрос, data_request_id обязателен
```

**Новая модель ExportHistory:**
```python
class ExportHistory(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    import_history_id = db.Column(db.Integer, db.ForeignKey('import_history.id'))
    products_count = db.Column(db.Integer, default=0)
    exported_at = db.Column(db.DateTime, default=datetime.utcnow)
    exported_by_id = db.Column(db.Integer, db.ForeignKey('users.id'))
    status = db.Column(db.String(20))  # success, failed, partial
    error_message = db.Column(db.Text)
    export_config = db.Column(db.JSON)
```

**Связь с товарами:**
```python
class Product(db.Model):
    # ... существующие поля ...
    import_history_id = db.Column(db.Integer, db.ForeignKey('import_history.id'))
    is_exported = db.Column(db.Boolean, default=False)
    exported_at = db.Column(db.DateTime)
```

### 3.7. Workflow (жизненный цикл товара)

#### 3.7.1. Статусы товаров

| Статус | Код | Описание |
|--------|-----|----------|
| Черновик | `draft` | Товар создан, но не обработан |
| В работе | `in_progress` | Товар находится в процессе обработки |
| На проверке | `to_review` | Товар требует ручной проверки |
| Утвержден | `approved` | Товар прошел все проверки и готов к экспорту |
| Отклонен | `rejected` | Товар отклонен по причине несоответствия требованиям |
| Экспортирован | `exported` | Товар отправлен в основную БД |

#### 3.6.2. Правила переходов

**Автоматические переходы:**
- `draft → in_progress`: автоматически сразу после создания товара при импорте
- `in_progress → to_review`: если оценка верификации 50-79% (требуется ручная проверка)
- `in_progress → approved`: если оценка верификации ≥80% (автоматическое утверждение)
- `in_progress → rejected`: если оценка верификации <50% или критические ошибки (отсутствуют обязательные поля, дублирование SKU)

**Ручные переходы:**
- `to_review → approved`: ручное утверждение менеджером
- `to_review → rejected`: ручное отклонение менеджером
- `rejected → in_progress`: возврат на доработку
- `approved → exported`: экспорт в основную БД (будущее)

**Ограничения:**
- Нельзя перейти из `exported` в другой статус
- Нельзя утвердить товар с оценкой <50%
- Нельзя утвердить товар с незаполненными обязательными полями

#### 3.6.3. История изменений статусов

**Сохранение:**
- Объект `ProductStatusHistory` для каждого перехода
- Поля:
  - `product_id` - товар
  - `old_status` - предыдущий статус
  - `new_status` - новый статус
  - `changed_by_id` - пользователь, изменивший статус
  - `changed_at` - время изменения
  - `comment` - комментарий (опционально)

**Отображение:**
- Хронологический список всех переходов
- Информация о пользователе и времени
- Комментарии к переходам

### 3.7. Версионирование товаров

#### 3.7.1. Создание версий

**Автоматическое создание:**
- При импорте товара
- При изменении любого атрибута
- При изменении статуса (опционально)

**Поля версии:**
- `product_id` - товар
- `version_number` - номер версии (автоинкремент)
- `data` - JSON с данными товара на момент версии
- `created_by_id` - пользователь, создавший версию
- `created_at` - время создания
- `comment` - комментарий (опционально)

#### 3.7.2. Просмотр версий

**Функционал:**
- ✅ Просмотр всех версий товара
- ✅ Сравнение версий
- ✅ Просмотр изменений между версиями
- ✅ Откат к предыдущей версии (опционально)

### 3.8. Экспорт в основную БД

#### 3.8.1. Назначение

Экспорт данных о товарах из системы в основную производственную базу данных для использования в продажах и других процессах.

#### 3.8.2. Процесс экспорта

**Условия экспорта:**
- Товар должен быть в статусе `approved` (утвержден)
- Товар должен пройти верификацию с оценкой ≥80%
- Все обязательные атрибуты должны быть заполнены
- Медиа-контент должен быть валидным (если требуется)

**Процесс:**
1. Выбор товаров для экспорта:
   - По файлу импорта (все товары из файла)
   - По запросу данных (все товары из запроса)
   - По подкатегории
   - По поставщику
   - Вручную (выбор конкретных товаров)
2. Валидация перед экспортом:
   - Проверка статусов товаров
   - Проверка полноты данных
   - Проверка верификации
3. Формирование данных для экспорта:
   - Преобразование данных в формат основной БД
   - Маппинг атрибутов на поля основной БД
   - Валидация формата данных
4. Экспорт в основную БД:
   - Создание записи в `ExportHistory`
   - Отправка данных в основную БД (API или прямой доступ)
   - Обновление статусов товаров на `exported`
   - Обновление статуса файла на `exported`
5. Обработка результатов:
   - Успешный экспорт: товары помечаются как `exported`
   - Частичный экспорт: создается `ExportHistory` со статусом `partial`
   - Ошибка экспорта: создается `ExportHistory` со статусом `failed`, товары остаются в статусе `approved`

#### 3.8.3. Формат экспорта

**Структура данных:**
- JSON формат (по умолчанию)
- XML формат (опционально)
- Прямая запись в БД (опционально)

**Поля экспорта:**
- Базовые поля товара (SKU, название, описание)
- Все атрибуты товара
- Медиа-контент (ссылки на изображения и 3D модели)
- Метаданные (поставщик, категория, подкатегория)

#### 3.8.4. Обработка ошибок

**Типы ошибок:**
- Ошибка подключения к основной БД
- Ошибка валидации данных
- Ошибка формата данных
- Конфликт данных (дублирование SKU в основной БД)

**Обработка:**
- Логирование всех ошибок
- Создание записи в `ExportHistory` с описанием ошибки
- Уведомление пользователя об ошибках
- Возможность повторить экспорт после исправления ошибок

#### 3.8.5. Откат экспорта

**Возможность отката:**
- Только для администраторов
- Только если товары еще не использованы в основной БД
- При откате:
  - Товары возвращаются в статус `approved`
  - Файл возвращается в статус `in_catalog`
  - Создается запись об откате в `ExportHistory`

#### 3.8.6. Модель ExportHistory

```python
class ExportHistory(db.Model):
    """История экспорта данных в основную БД"""
    __tablename__ = 'export_history'
    
    id = db.Column(db.Integer, primary_key=True)
    import_history_id = db.Column(db.Integer, db.ForeignKey('import_history.id'), nullable=True)
    data_request_id = db.Column(db.Integer, db.ForeignKey('data_requests.id'), nullable=True)
    
    # Какие товары экспортированы
    products_count = db.Column(db.Integer, default=0)
    products_ids = db.Column(db.JSON)  # Список ID товаров
    
    # Когда и кем экспортировано
    exported_at = db.Column(db.DateTime, default=datetime.utcnow)
    exported_by_id = db.Column(db.Integer, db.ForeignKey('users.id'))
    
    # Результаты экспорта
    status = db.Column(db.String(20))  # success, failed, partial
    error_message = db.Column(db.Text)
    
    # Метаданные экспорта
    export_config = db.Column(db.JSON)  # Конфигурация экспорта (формат, маппинг)
    export_format = db.Column(db.String(20))  # json, xml, direct_db
    
    # Откат
    is_rolled_back = db.Column(db.Boolean, default=False)
    rolled_back_at = db.Column(db.DateTime)
    rolled_back_by_id = db.Column(db.Integer, db.ForeignKey('users.id'))
```

### 3.9. Управление медиа-контентом

#### 3.8.1. Автоматическое скачивание

**Функционал:**
- ✅ Автоматическое скачивание изображений по URL
- ✅ Автоматическое скачивание 3D моделей по URL
- ✅ Сохранение в локальное хранилище
- ✅ Генерация уникальных имен файлов
- ✅ Валидация перед скачиванием

**Структура хранения:**
```
media/
├── images/
│   └── product_{id}/
│       ├── image_1.jpg
│       └── image_2.jpg
└── models/
    └── product_{id}/
        └── model.glb
```

#### 3.8.2. Просмотр медиа

**Функционал:**
- ✅ Просмотр изображений в галерее
- ✅ Просмотр 3D моделей (через model-viewer)
- ✅ Скачивание медиа-файлов
- ✅ Удаление медиа-файлов

### 3.10. Поиск и фильтрация

#### 3.9.1. Товары

**Фильтры:**
- По поставщику
- По подкатегории
- По статусу
- По оценке верификации
- По дате создания/обновления

**Поиск:**
- По названию товара
- По SKU
- По значениям атрибутов

**Сортировка:**
- По дате создания
- По дате обновления
- По названию
- По SKU
- По оценке верификации

#### 3.9.2. Другие сущности

**Категории:**
- Поиск по названию, коду
- Фильтрация по статусу активности

**Поставщики:**
- Поиск по названию, коду, email
- Фильтрация по категории

**Подкатегории:**
- Поиск по названию, коду
- Фильтрация по категории, поставщику

**Атрибуты:**
- Поиск по названию, коду
- Фильтрация по типу

---

## 4. ТРЕБОВАНИЯ К ИНТЕРФЕЙСУ

### 4.1. Общие принципы UX/UI

#### 4.1.1. Минималистичный дизайн
- **Чистый интерфейс** без лишних элементов
- **Монохромная палитра** с одним акцентным цветом
- **Минимальные тени** и эффекты
- **Быстрые переходы** (150ms)

#### 4.1.2. Понятная навигация
- **Иерархическая структура** меню
- **Хлебные крошки** для навигации
- **Активные состояния** для текущей страницы
- **Быстрый доступ** к основным функциям

#### 4.1.3. Информационная архитектура
- **Главная страница** - дашборд с категориями и статистикой
- **Разделы**: Товары, Импорт, Настройки
- **Логическая группировка** функций

### 4.2. Главная страница (Дашборд)

**Структура:**
1. **Статистика** (краткая):
   - Количество категорий
   - Количество поставщиков
   - Количество товаров
   - Средняя оценка верификации

2. **Категории товаров** (аккордеон):
   - Заголовок категории с кодом и названием
   - Статистика по категории:
     - Количество подкатегорий
     - Количество поставщиков
     - Количество товаров
     - Средняя оценка верификации
   - Поставщики в категории (раскрывающийся список):
     - Название поставщика
     - Количество подкатегорий
     - Количество товаров
     - Статистика по статусам товаров
     - Подкатегории поставщика (раскрывающийся список):
       - Название подкатегории
       - Количество товаров
       - Статистика по статусам
       - Ссылки на товары

3. **Вкладки workflow данных** (основной контент страницы):
   
   **Вкладка 1: Сбор данных**
   - **Назначение:** Управление процессом получения данных от поставщиков от регистрации до получения файлов
   - **Статистика вкладки:**
     - Количество поставщиков
     - Количество активных запросов
     - Количество полученных данных
     - Количество ожидающих ответа
   - **Разделы:**
     - **Поставщики:**
       - Таблица/карточки поставщиков с информацией: код, название, категории, запросы, статус
       - Индикаторы статуса: 🟢 Есть данные, 🟡 Ожидают ответа, 🔴 Нет ответа, ⚪ Новый
       - Действия: отправить запрос, скачать шаблон, редактировать, просмотреть
       - Фильтры: по категориям, статусу запросов, наличию данных
     - **Запросы данных:**
       - Таблица запросов с информацией: поставщик, категория, подкатегории, статус, срок, даты
       - Badges статусов: новый, отправлен, получено, нет ответа, отменен
       - Индикация просроченных сроков (красный цвет, иконка ⚠️)
       - Действия: отметить получено, просмотреть, редактировать, отменить, повторить
       - Фильтры: по поставщику, категории, статусу, сроку
   - **Быстрые действия:**
     - Создать поставщика (быстрое создание в модальном окне)
     - Отправить запрос (модальное окно с выбором поставщика, категорий, подкатегорий, срока)
     - Скачать шаблон (для выбранного поставщика и категории)
   
   **Вкладка 2: В обработке**
   - Файлы, которые импортируются и готовятся
   - Таблица файлов с прогрессом импорта
   - Информация: название, поставщик, запрос данных (ссылка), прогресс, статус
   - Индикаторы: прогресс-бар, количество обработанных строк
   - Действия: отменить, просмотреть логи, повторить
   - Фильтры: по поставщику, запросу данных, статусу
   
   **Вкладка 3: В каталоге**
   - Импорты, данные которых загружены в каталог
   - Таблица импортов с информацией о товарах
   - Информация: файл, поставщик, запрос данных (ссылка), количество товаров, дата импорта, оценка верификации
   - Действия: просмотреть товары, экспортировать в БД, повторная верификация
   - Фильтры: по поставщику, запросу данных, оценке верификации, статусу товаров
   
   **Вкладка 4: Экспортировано**
   - Экспорты данных в основную БД
   - Таблица экспортов
   - Информация: файл, поставщик, запрос данных (ссылка), количество товаров, дата экспорта, статус
   - Действия: просмотреть детали, повторить экспорт, откатить
   - Фильтры: по поставщику, запросу данных, дате экспорта, статусу
   
   **Общие элементы всех вкладок:**
   - Таблица данных с фильтрами и поиском
   - Статистика по вкладке
   - Действия для элементов
   - Сортировка

4. **Быстрые действия**:
   - Создать категорию
   - Создать поставщика
   - Импорт товаров
   - Просмотр товаров

### 4.3. Страницы списков

**Общие элементы:**
- Заголовок страницы с описанием
- Кнопка "Добавить" (справа)
- Поиск и фильтры (сверху)
- Таблица с данными
- Пагинация (если нужно)

**Таблицы:**
- Минималистичный дизайн
- Чередование строк (zebra striping)
- Hover эффекты
- Действия в последней колонке
- Иконки для статусов

### 4.4. Страницы форм

**Общие элементы:**
- Заголовок формы
- Поля с подсказками
- Валидация в реальном времени
- Кнопки "Сохранить" и "Отмена"
- Сообщения об ошибках

**Особенности:**
- Автоматическое заполнение некоторых полей (коды)
- Подсказки для обязательных полей
- Валидация перед отправкой

### 4.5. Страница товара

**Структура:**
1. **Заголовок**:
   - Название товара
   - SKU
   - Статус (badge)
   - Действия (изменить статус, редактировать)

2. **Информация о товаре**:
   - Подкатегория
   - Поставщик
   - Категория
   - Даты создания/обновления

3. **Атрибуты**:
   - Таблица с атрибутами и значениями
   - Обязательные атрибуты помечены
   - Отсутствующие атрибуты выделены

4. **Верификация**:
   - Общая оценка (прогресс-бар)
   - Детализация по категориям
   - Список проблем
   - История верификаций

5. **Медиа-контент**:
   - Галерея изображений
   - 3D модели (если есть)

6. **История**:
   - История версий
   - История изменений статусов

### 4.6. Страница импорта

**Структура:**
1. Выбор поставщика и подкатегории
2. Загрузка файла
3. Предпросмотр данных (опционально)
4. Настройка маппинга (если нужно)
5. Запуск импорта
6. Результаты импорта

### 4.7. Адаптивность

**Требования:**
- ✅ Работа на десктопе (1920px+)
- ✅ Работа на планшете (768px+)
- ⚠️ Мобильная версия (требует доработки)

---

## 5. ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### 5.1. Технологический стек

**Backend:**
- Python 3.10+
- Flask 2.3+
- SQLAlchemy 2.0+
- Flask-Migrate
- Flask-Login
- Flask-WTF (CSRF защита)

**База данных:**
- SQLite (разработка)
- PostgreSQL (production)

**Frontend:**
- HTML5
- CSS3 (Bootstrap 5)
- JavaScript (ES6+)
- Bootstrap Icons

**Библиотеки:**
- Pandas (обработка данных)
- Openpyxl (Excel)
- Pillow (изображения)
- Requests (HTTP)

### 5.2. Архитектура приложения

**Структура:**
```
app/
├── __init__.py          # Инициализация приложения
├── models/              # Модели данных
├── services/            # Бизнес-логика
├── forms/               # Формы
├── templates/           # Шаблоны
├── static/              # Статические файлы
├── api/                 # REST API
├── main/                # Основные роуты
├── auth/                # Аутентификация
└── import_data/         # Импорт данных
```

**Принципы:**
- Разделение ответственности (MVC)
- Сервисный слой для бизнес-логики
- RESTful API
- Миграции базы данных

### 5.3. Безопасность

**Требования:**
- ✅ Аутентификация пользователей
- ✅ CSRF защита
- ✅ Валидация всех входных данных
- ✅ Защита от SQL-инъекций (через ORM)
- ✅ Безопасное хранение паролей (хеширование)
- ⚠️ Расширенная система прав доступа (требует доработки)

### 5.4. Производительность

**Требования:**
- Обработка файлов до 10MB
- Импорт до 1000 товаров за раз
- Верификация товара < 5 секунд
- Отклик интерфейса < 200ms

**Оптимизация:**
- Индексы в базе данных
- Ленивая загрузка связей
- Кэширование (будущее)
- Асинхронная обработка (будущее)

### 5.5. Масштабируемость

**Текущие ограничения:**
- Работа с сотнями поставщиков
- Работа с тысячами товаров
- Работа с десятками категорий

**Будущие улучшения:**
- Поддержка миллионов товаров
- Распределенная обработка
- Микросервисная архитектура

---

## 6. ИНТЕГРАЦИИ

### 6.1. Текущие интеграции

- ✅ Импорт из Excel/CSV/JSON
- ✅ Экспорт шаблонов в Excel
- ✅ Скачивание медиа-файлов по URL

### 6.2. Планируемые интеграции

- ⚠️ Экспорт в основную БД
- ⚠️ API для внешних систем
- ⚠️ Интеграция с системами поставщиков
- ⚠️ Интеграция с каналами продаж

---

## 7. МОНИТОРИНГ И ЛОГИРОВАНИЕ

### 7.1. Логирование

**Уровни:**
- INFO - общая информация
- WARNING - предупреждения
- ERROR - ошибки
- CRITICAL - критические ошибки

**События:**
- Импорт товаров
- Верификация товаров
- Изменение статусов
- Ошибки валидации

### 7.2. Мониторинг

**Метрики:**
- Количество импортированных товаров
- Количество верифицированных товаров
- Средняя оценка верификации
- Время обработки

**Health Check:**
- Endpoint `/health` для проверки состояния системы
- Проверка подключения к БД
- Проверка доступности медиа-хранилища

---

## 8. ТЕСТИРОВАНИЕ

### 8.1. Типы тестов

**Unit тесты:**
- Тестирование моделей
- Тестирование сервисов
- Тестирование утилит

**Integration тесты:**
- Тестирование API
- Тестирование импорта
- Тестирование верификации

**E2E тесты:**
- Тестирование пользовательских сценариев
- Тестирование workflow

### 8.2. Покрытие тестами

**Текущее покрытие:** 100% основных функций

**Требования:**
- Минимум 80% покрытия кода
- Все критические функции покрыты тестами
- Регрессионное тестирование

---

## 9. ДОКУМЕНТАЦИЯ

### 9.1. Техническая документация

- ✅ API документация
- ✅ Документация моделей
- ✅ Документация сервисов
- ⚠️ Архитектурная документация (требует доработки)

### 9.2. Пользовательская документация

- ✅ Инструкции по использованию
- ✅ Руководство по импорту
- ✅ Руководство по верификации
- ⚠️ Видео-туториалы (будущее)

---

## 10. ПЛАН РАЗВИТИЯ

### 10.1. Ближайшие задачи (Q1)

1. **Улучшение UX/UI**:
   - Редизайн главной страницы
   - Улучшение навигации
   - Оптимизация форм

2. **Расширение функционала**:
   - Массовые операции с товарами
   - Расширенная аналитика
   - Экспорт отчетов

3. **Производительность**:
   - Оптимизация запросов к БД
   - Кэширование
   - Асинхронная обработка

### 10.2. Среднесрочные задачи (Q2-Q3)

1. **Экспорт данных**:
   - Экспорт в основную БД
   - Настройка каналов экспорта
   - Планирование экспорта

2. **Интеграции**:
   - API для внешних систем
   - Интеграция с системами поставщиков
   - Webhook'и для уведомлений

3. **Расширенная аналитика**:
   - Дашборды с графиками
   - Отчеты по поставщикам
   - Отчеты по категориям

### 10.3. Долгосрочные задачи (Q4+)

1. **Масштабирование**:
   - Поддержка миллионов товаров
   - Распределенная обработка
   - Микросервисная архитектура

2. **Расширенные функции**:
   - Мультиязычность
   - Расширенная система прав доступа
   - Рабочие процессы (workflows)

3. **AI/ML**:
   - Автоматическое определение категорий
   - Предложение атрибутов
   - Обнаружение аномалий

---

## 11. ГЛОССАРИЙ

- **PIM (Product Information Management)** - система управления информацией о товарах
- **Категория товаров** - верхний уровень иерархии, группирует товары по типам
- **Подкатегория** - группировка товаров внутри категории, определяет набор эталонных атрибутов
- **Поставщик** - компания или организация, поставляющая товары
- **Атрибут** - характеристика товара (название, цена, цвет и т.д.)
- **Эталонные свойства** - обязательный набор атрибутов для подкатегории
- **Верификация** - процесс проверки полноты и качества данных о товаре
- **Workflow** - жизненный цикл товара, управление статусами
- **Версионирование** - сохранение истории изменений товара
- **Маппинг полей** - сопоставление полей из файла поставщика с атрибутами системы
- **SKU (Stock Keeping Unit)** - артикул производителя, уникальный идентификатор товара

---

**Дата создания:** 2024  
**Версия:** 2.0  
**Статус:** Production Ready  
**Автор:** Development Team

