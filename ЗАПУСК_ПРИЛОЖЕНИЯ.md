# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ПРИЛОЖЕНИЯ

## ✅ ШАГИ ДЛЯ ЗАПУСКА

### 1. Установить зависимости (если еще не установлены)
```bash
pip install -r requirements.txt
```

### 2. Инициализировать базу данных
```bash
# Создать папку migrations (только первый раз)
flask db init

# Создать миграцию
flask db migrate -m "Initial migration"

# Применить миграцию (создать таблицы)
flask db upgrade

# Создать администратора
python run.py init-db
```

### 3. Запустить приложение
```bash
python run.py
```

Вы увидите:
```
 * Running on http://127.0.0.1:5000
 * Running on http://localhost:5000
Press CTRL+C to quit
```

### 4. Открыть в браузере
- **Адрес:** http://localhost:5000
- **Логин:** admin
- **Пароль:** admin

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: "ModuleNotFoundError"
**Решение:** Установите зависимости
```bash
pip install -r requirements.txt
```

### Проблема: "No such command 'db'"
**Решение:** Установите Flask-Migrate
```bash
pip install Flask-Migrate
```

### Проблема: "Port 5000 already in use"
**Решение 1:** Закройте другое приложение на порту 5000
**Решение 2:** Измените порт в `run.py`:
```python
app.run(debug=True, port=5001)
```

### Проблема: "Database not initialized"
**Решение:** Выполните инициализацию БД (шаг 2 выше)

### Проблема: Страница не загружается
**Проверьте:**
1. Приложение запущено (видите сообщение в консоли)
2. Правильный адрес: http://localhost:5000
3. Firewall не блокирует порт
4. Попробуйте другой браузер

---

## 🔍 ПРОВЕРКА РАБОТЫ

После запуска проверьте:
1. ✅ В консоли нет ошибок
2. ✅ Видите сообщение "Running on http://localhost:5000"
3. ✅ Страница загружается в браузере
4. ✅ Можете войти с admin/admin

---

## 📝 ПРИМЕЧАНИЯ

- Приложение запускается в режиме отладки (debug=True)
- При изменении кода приложение автоматически перезагружается
- Для остановки нажмите CTRL+C в консоли

---

**Готово! Приложение должно работать!** 🎉

