# ПЛАН АДАПТАЦИИ SALEOR ПОД ТРЕБОВАНИЯ ТЗ

## ОЦЕНКА СООТВЕТСТВИЯ SALEOR ТРЕБОВАНИЯМ ТЗ

### ✅ ЧТО УЖЕ ЕСТЬ В SALEOR (ИСПОЛЬЗУЕМ БЕЗ ИЗМЕНЕНИЙ):

1. **Технологический стек:**
   - ✅ Python (Django) - очень близко к Flask из ТЗ
   - ✅ PostgreSQL - точно как в ТЗ
   - ✅ Django ORM - похож на SQLAlchemy по концепции
   - ✅ REST API возможности (через GraphQL или можно добавить REST)

2. **Управление продуктами:**
   - ✅ Модель Product (товар)
   - ✅ Модель Category (категория)
   - ✅ Система атрибутов (ProductAttribute, AttributeValue)
   - ✅ Типы атрибутов (текст, число, выбор и т.д.)
   - ✅ Обязательные/опциональные атрибуты
   - ✅ Медиа-контент (изображения продуктов)

3. **Базовая функциональность:**
   - ✅ Аутентификация и авторизация
   - ✅ Админ-панель Django
   - ✅ API для работы с данными
   - ✅ Валидация данных
   - ✅ История изменений (через Django admin или можно добавить)

---

### ⚠️ ЧТО НУЖНО ДОБАВИТЬ/АДАПТИРОВАТЬ:

#### 1. ИЕРАРХИЧЕСКАЯ СТРУКТУРА

**Требование ТЗ:**
```
Category → Supplier → Subcategory → Product
```

**Текущее в Saleor:**
```
Category → Product
```

**Что нужно добавить:**
- Модель `Supplier` (поставщик)
- Модель `Subcategory` (подкатегория)
- Связи между моделями
- Адаптировать существующую модель `Category` под требования

---

#### 2. ИМПОРТ ДАННЫХ

**Требование ТЗ:**
- Импорт из Excel (.xlsx, .xls)
- Импорт из CSV
- Импорт из JSON
- Маппинг полей
- Валидация при импорте

**Текущее в Saleor:**
- GraphQL API для создания продуктов
- Нет встроенного импорта из файлов

**Что нужно добавить:**
- Django management команды для импорта
- Обработка Excel (библиотека `pandas` или `openpyxl`)
- Обработка CSV (встроенный `csv` модуль Python)
- Обработка JSON (встроенный `json` модуль)
- Система маппинга полей
- Валидация данных при импорте

---

#### 3. ВЕРИФИКАЦИЯ ДАННЫХ

**Требование ТЗ:**
- Проверка полноты данных (0-100%)
- Проверка качества данных (0-100%)
- Проверка медиа-контента (0-100%)
- Общая оценка товара (0-100%)
- Список проблем (issues)

**Текущее в Saleor:**
- Базовая валидация через Django forms
- Нет системы верификации

**Что нужно добавить:**
- Модель `ProductVerification` для хранения результатов
- Модель `VerificationIssue` для списка проблем
- Логика проверки полноты данных
- Логика проверки качества данных
- Логика проверки медиа-контента
- Расчет оценок
- API для запуска верификации

---

#### 4. WORKFLOW СИСТЕМА

**Требование ТЗ:**
Статусы:
- draft (черновик)
- in_progress (в работе)
- to_review (на проверке)
- approved (утвержден)
- rejected (отклонен)
- exported (экспортирован)

**Текущее в Saleor:**
- Нет встроенной workflow системы
- Можно добавить поле `status` в модель Product

**Что нужно добавить:**
- Поле `status` в модель Product
- Модель `ProductStatusHistory` для истории переходов
- Логика автоматических переходов на основе верификации
- API для ручных переходов
- Валидация возможности перехода между статусами

---

#### 5. ВЕРСИОНИРОВАНИЕ

**Требование ТЗ:**
- Каждое изменение создает новую версию
- История всех версий
- Отслеживание автора изменений
- Комментарии к версиям

**Текущее в Saleor:**
- Django admin логирует изменения
- Нет явной системы версионирования

**Что нужно добавить:**
- Использовать библиотеку `django-reversion` или создать свою систему
- Модель `ProductVersion` для хранения версий
- Автоматическое создание версий при изменении
- API для просмотра версий
- Возможность отката к предыдущей версии

---

#### 6. ЭТАЛОННЫЕ СВОЙСТВА

**Требование ТЗ:**
- Каждая подкатегория имеет набор эталонных атрибутов
- Атрибуты из глобального справочника
- Обязательность заполнения для каждого атрибута в подкатегории
- Порядок сортировки атрибутов

**Текущее в Saleor:**
- Есть глобальный справочник атрибутов
- Атрибуты привязаны к типам продуктов

**Что нужно адаптировать:**
- Связать атрибуты с подкатегориями (через модель `SubcategoryAttribute`)
- Добавить обязательность и порядок сортировки
- Использовать эталонные свойства при импорте и верификации

---

#### 7. FRONTEND

**Требование ТЗ:**
- HTML, CSS (Bootstrap 5), JavaScript
- Веб-интерфейс для всех операций

**Текущее в Saleor:**
- GraphQL API
- React frontend (Dashboard)
- Можно использовать Django templates

**Что нужно сделать:**
- Создать Django views и templates
- Использовать Bootstrap 5 для стилизации
- Адаптировать существующий React dashboard или создать новый на Bootstrap

---

## ПЛАН РЕАЛИЗАЦИИ

### ЭТАП 1: УСТАНОВКА И БАЗОВАЯ НАСТРОЙКА (1-2 дня)

1. **Установка Saleor:**
   ```bash
   git clone https://github.com/saleor/saleor.git
   cd saleor
   pip install -r requirements.txt
   ```

2. **Настройка базы данных:**
   - Настроить PostgreSQL
   - Выполнить миграции
   - Создать суперпользователя

3. **Изучение структуры:**
   - Изучить модели продуктов
   - Изучить систему атрибутов
   - Изучить API структуру

---

### ЭТАП 2: РАСШИРЕНИЕ МОДЕЛЕЙ ДАННЫХ (3-5 дней)

1. **Создать модель Supplier:**
   ```python
   class Supplier(models.Model):
       code = models.CharField(max_length=100, unique=True)
       name = models.CharField(max_length=255)
       category = models.ForeignKey(Category, on_delete=models.CASCADE)
       contact_person = models.CharField(max_length=255)
       email = models.EmailField()
       phone = models.CharField(max_length=50)
       address = models.TextField()
       # ...
   ```

2. **Создать модель Subcategory:**
   ```python
   class Subcategory(models.Model):
       code = models.CharField(max_length=100)
       name = models.CharField(max_length=255)
       supplier = models.ForeignKey(Supplier, on_delete=models.CASCADE)
       # ...
   ```

3. **Связать Product с новой иерархией:**
   - Добавить ForeignKey на Subcategory
   - Адаптировать существующие связи

4. **Создать модель SubcategoryAttribute:**
   ```python
   class SubcategoryAttribute(models.Model):
       subcategory = models.ForeignKey(Subcategory, on_delete=models.CASCADE)
       attribute = models.ForeignKey(ProductAttribute, on_delete=models.CASCADE)
       is_required = models.BooleanField(default=False)
       sort_order = models.IntegerField(default=0)
   ```

5. **Добавить статус в Product:**
   ```python
   STATUS_CHOICES = [
       ('draft', 'Черновик'),
       ('in_progress', 'В работе'),
       ('to_review', 'На проверке'),
       ('approved', 'Утвержден'),
       ('rejected', 'Отклонен'),
       ('exported', 'Экспортирован'),
   ]
   status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='draft')
   ```

---

### ЭТАП 3: СИСТЕМА ВЕРИФИКАЦИИ (5-7 дней)

1. **Создать модели для верификации:**
   ```python
   class ProductVerification(models.Model):
       product = models.ForeignKey(Product, on_delete=models.CASCADE)
       completeness_score = models.IntegerField(default=0)  # 0-100
       quality_score = models.IntegerField(default=0)  # 0-100
       media_score = models.IntegerField(default=0)  # 0-100
       overall_score = models.IntegerField(default=0)  # 0-100
       verified_at = models.DateTimeField(auto_now_add=True)
       # ...
   
   class VerificationIssue(models.Model):
       verification = models.ForeignKey(ProductVerification, on_delete=models.CASCADE)
       issue_type = models.CharField(max_length=50)  # missing, invalid, etc.
       attribute = models.ForeignKey(ProductAttribute, null=True, on_delete=models.SET_NULL)
       message = models.TextField()
   ```

2. **Реализовать логику верификации:**
   - Проверка полноты (обязательные атрибуты)
   - Проверка качества (валидация значений)
   - Проверка медиа-контента (изображения)
   - Расчет оценок

3. **Создать API для верификации:**
   - Эндпоинт для запуска верификации
   - Эндпоинт для получения результатов

---

### ЭТАП 4: WORKFLOW СИСТЕМА (3-4 дня)

1. **Создать модель истории статусов:**
   ```python
   class ProductStatusHistory(models.Model):
       product = models.ForeignKey(Product, on_delete=models.CASCADE)
       old_status = models.CharField(max_length=20)
       new_status = models.CharField(max_length=20)
       changed_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
       changed_at = models.DateTimeField(auto_now_add=True)
       comment = models.TextField(blank=True)
   ```

2. **Реализовать логику переходов:**
   - Автоматические переходы на основе верификации
   - Ручные переходы через API
   - Валидация возможности перехода

3. **Создать сигналы Django:**
   - Автоматический переход при импорте
   - Автоматический переход на основе оценки верификации

---

### ЭТАП 5: ВЕРСИОНИРОВАНИЕ (2-3 дня)

1. **Установить django-reversion:**
   ```bash
   pip install django-reversion
   ```

2. **Настроить версионирование:**
   - Зарегистрировать модели для версионирования
   - Настроить автоматическое создание версий

3. **Создать API для работы с версиями:**
   - Просмотр всех версий продукта
   - Откат к предыдущей версии

---

### ЭТАП 6: ИМПОРТ ДАННЫХ (5-7 дней)

1. **Установить необходимые библиотеки:**
   ```bash
   pip install pandas openpyxl
   ```

2. **Создать Django management команды:**
   ```python
   # management/commands/import_products.py
   class Command(BaseCommand):
       def add_arguments(self, parser):
           parser.add_argument('file', type=str)
           parser.add_argument('--supplier', type=int)
           parser.add_argument('--subcategory', type=int)
           parser.add_argument('--format', choices=['excel', 'csv', 'json'])
   ```

3. **Реализовать обработку файлов:**
   - Парсинг Excel (pandas)
   - Парсинг CSV (csv модуль)
   - Парсинг JSON (json модуль)

4. **Реализовать маппинг полей:**
   - Автоматический маппинг (по названиям)
   - Ручной маппинг (через конфигурацию)

5. **Создать веб-интерфейс для импорта:**
   - Форма загрузки файла
   - Настройка маппинга
   - Предпросмотр данных
   - Запуск импорта
   - Отображение результатов

---

### ЭТАП 7: FRONTEND НА BOOTSTRAP 5 (7-10 дней)

1. **Создать Django views:**
   - Список категорий
   - Список поставщиков
   - Список подкатегорий
   - Список товаров
   - Детали товара
   - Импорт данных
   - Верификация

2. **Создать Django templates:**
   - Базовый шаблон с Bootstrap 5
   - Шаблоны для всех страниц

3. **Добавить JavaScript:**
   - AJAX для динамических операций
   - Валидация форм
   - Интерактивные элементы

---

### ЭТАП 8: API (REST) (3-4 дня)

1. **Установить Django REST Framework:**
   ```bash
   pip install djangorestframework
   ```

2. **Создать сериализаторы:**
   - Для всех моделей
   - Валидация данных

3. **Создать ViewSets:**
   - CRUD операции для всех моделей
   - Специальные эндпоинты (импорт, верификация, workflow)

4. **Настроить роутинг:**
   - URL маршруты
   - Документация API

---

## ОЦЕНКА ВРЕМЕНИ

| Этап | Время | Приоритет |
|------|-------|-----------|
| Установка и настройка | 1-2 дня | Высокий |
| Расширение моделей | 3-5 дней | Высокий |
| Верификация | 5-7 дней | Высокий |
| Workflow | 3-4 дня | Высокий |
| Версионирование | 2-3 дня | Средний |
| Импорт данных | 5-7 дней | Высокий |
| Frontend | 7-10 дней | Высокий |
| REST API | 3-4 дня | Средний |
| **ИТОГО** | **29-42 дня** | |

**Оптимистичная оценка:** 4-5 недель  
**Реалистичная оценка:** 6-8 недель  
**С учетом тестирования:** 8-10 недель

---

## ПРЕИМУЩЕСТВА ИСПОЛЬЗОВАНИЯ SALEOR

1. ✅ **Готовая архитектура** - не нужно создавать с нуля
2. ✅ **Проверенный код** - используется в production
3. ✅ **Активное сообщество** - поддержка и обновления
4. ✅ **Хорошая документация** - легче разобраться
5. ✅ **Масштабируемость** - рассчитан на большие объемы
6. ✅ **Безопасность** - встроенные механизмы защиты
7. ✅ **Расширяемость** - легко добавлять новые функции

---

## РИСКИ И МИТИГАЦИЯ

### Риск 1: Saleor использует Django, а ТЗ требует Flask
**Митигация:** Django очень похож на Flask, концепции одинаковые. Можно адаптировать или использовать как есть.

### Риск 2: GraphQL вместо REST API
**Митигация:** Можно добавить Django REST Framework поверх существующего GraphQL API.

### Риск 3: React frontend вместо Bootstrap
**Митигация:** Можно создать отдельный Django templates frontend на Bootstrap 5, оставив React dashboard для админов.

### Риск 4: Сложность адаптации существующего кода
**Митигация:** Saleor имеет модульную структуру, легко расширяется через Django apps.

---

## ВЫВОД

**ДА, Saleor можно эффективно использовать для быстрого построения системы!**

**Преимущества:**
- Экономия времени (не нужно создавать с нуля)
- Готовая архитектура и лучшие практики
- Соответствие технологическому стеку (Python, PostgreSQL)
- Легко расширяется под требования ТЗ

**Что нужно:**
- Адаптировать модели данных (добавить Supplier, Subcategory)
- Добавить функционал (импорт, верификация, workflow)
- Создать frontend на Bootstrap 5
- Настроить REST API

**Оценка времени:** 6-8 недель для полноценной системы.

---

**Готов начать реализацию!** 🚀

